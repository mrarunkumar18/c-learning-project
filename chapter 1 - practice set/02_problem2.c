#include <stdio.h>

int main()
{

    int r = 6;
    int height = 10;
    printf("the area of circle with radious %f", r, 3.14 * r * r);
    printf("\nthe volume of cylinder with radious %d and height %d is %f", r, height, 3.14 * r * r * height);

    return 0;
}

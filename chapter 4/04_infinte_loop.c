#include <stdio.h>

int main()
{

    int i = 0;
    while (2 < 10)
    {
        printf("the value of i is %d\n", i);
        i++;
    }

    return 0;
}
// This program demonstrates an infinite loop. The condition in the while loop (2 < 10) is always true, so the loop will
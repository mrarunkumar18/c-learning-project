#include <stdio.h>

int main()
{

    int n = 45;
    float m = 32.23;
    n = (int)m; // typecasting // converting data type to int from float

    printf("the value of n is %d \n", n);
    return 0;
}